return {
    {
        -- edit directory in buffer for easy batch rename/etc
        "elihunter173/dirbuf.nvim",
        -- enabled = false,
        -- ft = { "dirbuf" },
        opts = {
            hash_padding = 2,
            show_hidden = true,
            sort_order = "default",
        },
    },
}
